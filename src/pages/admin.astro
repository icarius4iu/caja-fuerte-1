---
import MainLayout from "../layouts/MainLayout.astro";
import AdminDashboard from "../components/svelte/AdminDashboard.svelte";
---

<MainLayout title="Admin Panel - Caja Fuerte">
    <!-- Background overlay for admin -->
    <!-- Background overlay for admin -->
    <div class="fixed inset-0 z-0 pointer-events-none bg-[#0B0D11]">
        <!-- Tech Grid -->
        <div
            class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.03)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.03)_1px,transparent_1px)] bg-[size:40px_40px] [mask-image:radial-gradient(ellipse_at_center,black,transparent_80%)]"
        >
        </div>

        <!-- Glow Orbs -->
        <div
            class="fixed top-0 right-0 w-[500px] h-[500px] bg-primary/10 blur-[120px] rounded-full mix-blend-screen"
        >
        </div>
        <div
            class="fixed bottom-0 left-0 w-[500px] h-[500px] bg-secondary/10 blur-[120px] rounded-full mix-blend-screen"
        >
        </div>
    </div>

    <div class="relative z-10 pt-20">
        <AdminDashboard client:load />
    </div>
</MainLayout>
